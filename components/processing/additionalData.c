#include "additionalData.h"
#include <string.h>

// Inicializar los valores por defecto
additionalData_t add = {
    .mode = 0,
    .glss_svs = 0,
    .beid_svs = 0,
    .alt = 0.0,
    .pdop = 0.0,
    .hdop = 0.0,
    .vdop = 0.0,
};